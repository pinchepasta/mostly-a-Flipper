build/
build_t_embed/
build_test/
build_s3/
.cache/
.idea/
.claude/
__pycache__/
sdkconfig.old
.DS_Store
build_waveshare_c6/
build_cardputer/
build_cardputer_adv/
.vscode/settings.json

# Auto-generated by idf.py/winbuild.py; source of truth is sdkconfig.defaults*
sdkconfig
generated/

# Local-dev tooling (not for upstream)
_flash_helper.py

# Multi-boot: the Bruce firmware checkout is managed by patchBruce.py
# (clone + git pull + tools/bruce_multiboot.patch), not checked in.
multi-boot/bruce/

# Managed components are pulled by IDF component manager from idf_component.yml
# + dependencies.lock; the source-of-truth is those two files.
managed_components/

# Local development snapshots and generated release artifacts
.backups/
*.before-*
*.after-*
*.bak
*.orig
*.rej
*~
releases/*.zip
*-backup-*
*.backup-*

# Merged flashable images (reproducible from build_<board>/ via esptool merge_bin)
*-merged.bin
