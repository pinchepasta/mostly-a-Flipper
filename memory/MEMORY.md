# Memory index — C:\Users\Eli\Downloads\Flipper (M5Stack Cardputer-ADV Flipper Zero ESP32 port)

Project = Cardputer + Cardputer-ADV support (from 0xhalloween) grafted onto the Sor3nt Flipper-Zero-ESP32-Port base. Focus board: **m5stack_cardputer_adv** (ESP32-S3FN8, 8MB flash, **no PSRAM**, ~200KB heap). Now a git repo (`master`); work is committed per phase.

- [Flipper Cardputer merge](flipper-cardputer-merge.md) — what the project is, how the Cardputer graft was assembled, and the original merge/on-hardware bugs fixed (per-board sdkconfig, partition table, LCD/SD SPI, TCA8418 keyboard, ADV shared-SPI3 layout).
- [Flipper ESP-IDF build env](flipper-esp-idf-build-env.md) — how to build on this machine: ESP-IDF 5.4.1 at `C:\Espressif`, the Python 3.14 gotcha (must use bundled idf-python 3.11.2), `C:\Espressif\flipper_build.bat <board>`; flash/serial workflow (esptool via idf-python on **COM9**, USB-Serial-JTAG re-enumerates at app boot, cap*.py capture scripts, addr2line for backtraces).
- [Flipper duplicate app trees](flipper-duplicate-app-trees.md) — **components/ beats applications/ for duplicate appids** (live loader = `components/loader`, live BT = `components/btshim`, live BT settings = `components/btshim/bt_settings_app.c`); how Flipper-mobile BLE connect was made to work; the measured **BLE RAM ceiling** and why BLE can't be fully stable without PSRAM; the `ESP32_MIN_STACK_SIZE=4096` clamp.
- [Flipper Cardputer-ADV worklog](flipper-cardputer-adv-worklog.md) — the full 6-phase pass (2026-06-30/07-01) with per-phase commit hashes: driver hardening, Grove IR-module detect, Sor3nt parity, memory reclamation, **module "not available" UX (SubGHz/NFC/NRF24)**, and the validated **merged .bin** (`Flipper-cardputer_adv-merged.bin`, gitignored). Also: Bluetooth was deliberately DISABLED here — **but as of 2026-07-05 it's re-enabled behind a heap guard (see FAP/apps/quirks), no longer hard-off.**
- [Cardputer-ADV FAP/apps/quirks](flipper-cardputer-adv-fap-apps-quirks.md) — 2026-07-04/05: **`.fap`s can't run on no-PSRAM (loader is PSRAM-only) → INTERNALIZE apps instead**; KeyCopier internalized; **fam_config KeyError gotcha** when removing apps; examples + t_embed_snake removed; lfrfid re-enabled w/ "No module" refusal; FindMy furi_check-crash fix; TOTP empty-config-header fix; **TFT idle backlight floor — raised 77→219/255 (~86%) on 2026-07-05: 30% is actually BLACK on the real unit, panel only lights above ~80-85% duty**; **idle→ESP32 light sleep after 2 min (furi_hal_power_light_sleep, wake on key/BOOT, USB-inhibit, force-wake fallback)**; serial needs `DtrEnable=$true`; failed-configure corrupts build dir (rmdir + rebuild). Also: user runs Gemini on this repo in parallel.
- [Beta firmware push-only](beta-firmware-push-only.md) — **new bin → push ONLY the `Beta_Firmware/` folder to GitHub (origin/Main), not source, until the user says "push all files".** **2026-07-05 (later): GPIO app got a "Custom Pin (any GPIO)" scene (runtime pin select/mode/drive/read, excludes flash 26-32) — `applications/main/gpio/scenes/gpio_scene_custom.c`.** **2026-07-05: BT crash-on-enable now guarded (`BT_MIN_FREE_INTERNAL` 72KB, no more OOM reboot); WiFi soft-reset warning dialog; NRF24 jammer CE pin 43→4 fix; GPIO app internalized (Phase-1 manual control, Grove GPIO2/GPIO1, editable table in furi_hal_resources.c, USB-UART bridge deferred); gotcha: internalizing an app needs `#include <furi.h>`.**
- [Cardputer-ADV audio + FIDO](flipper-cardputer-adv-audio-fido.md) — 2026-07-06: **ADV mic is the ES8311 codec ADC, NOT PDM** (board `MIC_DATA/CLK` macros are misleading); Recorder app (main menu, mic→WAV on `/ext/recordings`) via full-duplex I2S + `furi_hal_mic.c`; mic gain tuned in `furi_hal_speaker.c` `es8311_adc_seq` (regs 0x16/0x17). **ZeroFIDO internalized** (FIDO2 passkey over USB CTAPHID) needing a new U2F-HID TinyUSB transport (0xF1D0) + a **RX FIFO** for multi-packet PIN. Dolphin state moved `INT_PATH`→`EXT_PATH` (SD, RTC-cached).
